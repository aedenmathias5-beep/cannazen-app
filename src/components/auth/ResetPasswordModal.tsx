export default function ResetPasswordModal({ onClose, onSwitch }: { onClose: () => void; onSwitch: (m: string) => void }) {
  return <div style={{ padding: '40px', textAlign: 'center' }}><p>Réinitialisation bientôt disponible</p></div>;
}
